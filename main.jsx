import React from "react";
import ReactDOM from "react-dom/client";
import JetemaWorkspace from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JetemaWorkspace />
  </React.StrictMode>
);
