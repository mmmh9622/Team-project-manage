[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

# SPA fallback -- keep this AFTER the function's own /api/storage route,
# which Netlify resolves before falling through to redirects.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
